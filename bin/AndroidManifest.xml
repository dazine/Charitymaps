<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.charitymaps"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <permission
        android:name="com.charitymaps.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <!-- Required permissions -->

    <uses-permission android:name="com.charitymaps.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Used by the API to download map tiles from Google Maps servers: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Allows the API to access Google web-based services: -->
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!-- Allows the API to cache map tile data in the device's external storage area: -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Optional permissions -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Version 2 of the Google Maps Android API requires OpenGL ES version 2 -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme" >
        <activity android:name="com.charitymaps.Head" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" >
            <intent-filter>
                <action android:name="com.charitymaps.IntroActivity"/>

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GoogleMaps"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="com.charitymaps.MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- All disaster Activity -->
        <activity
            android:name=".AllDisastersActivity"
            android:label="Alle rampen" >
            <intent-filter>
                <action android:name="com.charitymaps.AllDisastersActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- View disaster Activity -->
        <activity
            android:name=".ViewDisasterActivity"
            android:label="Ramp bekijken" >
        </activity>
        <!-- All charity Activity -->
        <activity
            android:name=".AllCharityActivity"
            android:label="Alle stichtingen" >
            <intent-filter>
                <action android:name="com.charitymaps.AllCharityActivity" />

                <category android:name="android.intent.category.Default" />
            </intent-filter>
        </activity>

        <!-- View charity Activity -->
        <activity
            android:name=".ViewCharityActivity"
            android:label="Stichting bekijken" >
        </activity>
        <activity android:name="com.charitymaps.MainScreenActivity" >
            <intent-filter>
                <action android:name="com.charitymaps.MainScreenActivity" />

                <category android:name="android.intent.category.Default" />
            </intent-filter>
        </activity>
        <activity android:name=".Overzicht" >
            <intent-filter>
                <action android:name="com.charitymaps.Overzicht" />

                <category android:name="android.intent.category.Default" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyAfc16wshkW507co3zT6TqCFIGP2HlizUA" />
    </application>

</manifest>